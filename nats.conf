# NATS server configuration
# No per-message payload limit — large OSM relation geometries can exceed the
# default 1 MB ceiling, so we remove the cap entirely.
max_payload: 67108864  # 64 MB (hard maximum the NATS server supports)

jetstream {
  store_dir: /data
}

http_port: 8222
