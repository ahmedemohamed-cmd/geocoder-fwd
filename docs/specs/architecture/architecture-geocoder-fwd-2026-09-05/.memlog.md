---
scope: Geocoder platform domain: services/ + shared/ + the compose topology that runs them. Excludes billing/ (documented separately).
purpose: build-substrate
altitude: feature
updated: 2026-09-05T18:09
---

- (direction) Brownfield derive-from-code run. Fast path chosen by ezzat; deliverables = spine + fuller solution-design doc. Warts go to a separate gap register, not this spine.
- (assumption) Paradigm read off the code, not chosen: pipes-and-filters for ingest (NATS JetStream between stages) with a layered request/response serving app on top. [ASSUMPTION] the naming; the structure itself is verified.
- (constraint) VERIFIED layering: shared/ never imports services/ (grep clean); services/ imports shared/ freely; neither imports billing/. The dependency arrow is one-way and already holds.
- (decision) Durable document writes reach ES/PostGIS only via the NATS OSM stream. /insert and /places PUBLISH rather than write. es_inserter is the sole ES document writer; postgis_inserter the sole geometry writer. Prevents: a second write path that skips enrichment, dedup and the processed ledger.
- (decision) Two audited exceptions to the write rule, both partial field updates on existing docs, never creation: geocoder.py:1613 popularity via /feedback, geocoder.py:705 ai_description via /describe. Any NEW direct es.update is a violation.
- (decision) Store ownership is exclusive. ES osm_places = searchable documents. PostGIS = geometry + address points + the processed_files ledger. Redis = derived/ephemeral only (ac:* autocomplete, tf:* traffic edges, caches) and must be rebuildable from ES/PostGIS. Valhalla owns routing tiles. Prevents: two owners of one fact.
- (decision) Every serving-path component is replica-safe: no in-process singletons, no leader election, no per-instance local state. Existing mechanisms: shared durable NATS consumers, WORK_QUEUE stream for provider cells (exactly-once delivery), leaderless Redis SET NX scheduler, TRAFFIC_WRITER_SHARDS per-tile sharding, PROCESSED_LEDGER=pg atomic claims. [ADOPTED] stated as policy by ezzat.
- (decision) Config is read in exactly one place: shared/config.py, via _safe_int/_safe_bool/_safe_float. Enforced socially, not mechanically. Prevents: the OLLAMA_URL drift already found between shared/llm.py and shared/config.py.
- (decision) Logging goes through shared.logging.get_logger into the 'geocoder' logger tree with lazy %s args; print() is banned in services/ and shared/. Mechanically enforced by ruff G004 + T20.
- (constraint) Search quality is gated by docs/quality-baseline.md, not the unit suite. Any change to search, ranking, autocomplete, categories or the ES mapping must measure against it. @5/@10 may not drop; strict@1 may drop 1pp only with a written explanation.
- (version) Runtime: python 3.11-slim, fastapi >=0.110 <1.0, pydantic 2.x, uvicorn[standard], elasticsearch[async] 8.x, redis-py 5.x, asyncpg 0.29+, nats-py 2.6+, osmium 3.7+, sentence-transformers 2.2-4.0
- (version) Pinned images: elasticsearch 8.11.0, postgis 16-3.4-alpine, postgres 16-alpine, nats 2.10, redis 7-alpine, etcd v3.5.16, apisix 3.9.1-debian
- (question) UNPINNED images break reproducibility and contradict the pinning discipline elsewhere: ollama/ollama:latest, valhalla:latest, zitadel:latest, curlimages/curl:latest. Gap-register item.
- (question) services/geocoder.py is 2707 lines and holds the whole serving path except routing/ and nearby/, which are already APIRouters. The extraction pattern exists and is proven; the rest is deferred. Gap-register item, not a spine decision.
- (event) code sweep complete: layering, write paths, store ownership, stream topology, versions verified against the tree
- (event) distillation starting
- (event) Reviewer gate: lint_spine.py clean (0 findings). Two configured lenses applied inline rather than as subagents.
- (decision) AD-11 added by the adversarial lens: ac:* has three writers (warm_from_es, update_score via /feedback, index_entry via /places). All funnel through shared/autocomplete.py today, but nothing stated it — a new unit could zadd directly and corrupt entry encoding.
- (decision) AD-12 added by the adversarial lens: tf:e:{graphid} {kph,n,ts} is written by BOTH traffic_aggregator and routing.py on-demand fetch, and read by traffic_writer. Nothing fixed units or timestamp format; two units obeying every other AD could write km/h vs m/s. Also captures that the EWMA fold must stay server-side atomic.
- (question) traffic_writer.py docstring claims tf:e:* is 'written by traffic_aggregator, read here' — but routing.py also writes it. Stale docstring, gap-register item.
- (question) Reviewer lens 1 (version currency) not web-verified: versions were confirmed against the repo's own requirements.txt and compose file, which the lens permits, but ES 8.11.0 may be well behind current 8.x. Upgrade assessment is a gap-register item, not a spine decision.
- (event) reviewer gate resolved
- (event) doc_standards applied to SOLUTION-DESIGN.md: structure lens condensed the duplicated scaling list (README is the maintained source), added a diagram pointer; prose lens fixed 6 comprehension issues. Spine exempt from prose standards by design.
- (event) spine finalized
